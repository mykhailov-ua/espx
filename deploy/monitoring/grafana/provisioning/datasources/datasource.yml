# datasource.yml: Grafana auto-provisions Prometheus as the default metrics backend.
# URL points at the host-mapped Prometheus port used in docker compose monitoring profile.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9190
    isDefault: true
