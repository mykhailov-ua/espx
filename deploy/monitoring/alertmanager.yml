global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'telegram-proxy'

receivers:
- name: 'telegram-proxy'
  webhook_configs:
  - url: 'http://alertmanager-telegram:8222/alerts'
